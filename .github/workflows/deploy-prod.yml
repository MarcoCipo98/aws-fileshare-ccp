name: deploy-prod

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  prod-smoke:
    name: prod-smoke
    runs-on: ubuntu-latest

    steps:
      - name: PROD smoke test (/health)
        run: |
          curl -fsS <PROD_HEALTH_URL>
